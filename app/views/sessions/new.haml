%h1 LOGIN ROUTE

= form_tag "/login" do
	%fieldset
		= text_field :user, :email, placeholder: "Email"
	%fieldset
		= password_field :user, :password, placeholder: "Password"
	%fieldset.actions
		= submit_tag "Login"

= link_to "Don't have an account? Click to register", signup_url
