%h1 LOGIN ROUTE

= form_tag "/login" do
	%fieldset
		= text_field :user, :email, placeholder: "Email"
	%fieldset
		= password_field :user, :password, placeholder: "Password"
	%fieldset.actions
		= submit_tag "Login"
